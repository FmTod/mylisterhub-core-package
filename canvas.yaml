preset: package

namespace: MyListerHub\Core
user-auth-provider: App\User

paths:
  src: src
  resource: resources

factory:
  path: database/factories

migration:
  path: database/migrations
  prefix: ''

console:
  namespace: MyListerHub\Core\Console

model:
  namespace: MyListerHub\Core\Models

provider:
  namespace: MyListerHub\Core\Providers

testing:
  namespace: MyListerHub\Core\Tests
